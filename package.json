{
  "name": "hello-world",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://sophisticasean.github.io/meme_dashboard",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "axios": "^0.15.2",
    "rc-table": "^5.0.5",
    "react": "^15.3.2",
    "react-dom": "^15.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "run": "pushstate-server build 80",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  }
}
